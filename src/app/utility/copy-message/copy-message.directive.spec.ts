import { CopyMessageDirective } from './copy-message.directive';

describe('CopyMessageDirective', () => {
  it('should create an instance', () => {
    const directive = new CopyMessageDirective();
    expect(directive).toBeTruthy();
  });
});
